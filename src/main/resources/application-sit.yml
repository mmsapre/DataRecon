# SIT — SPRING_PROFILES_ACTIVE=sit (or PROFILE=sit)
# Datasources / domains / profiles / tags: define via API after start.
mms:
  recon:
    postgres:
      datasources: {}
    mongodb:
      datasources: {}
    bigquery:
      datasources: {}
    domains: {}
