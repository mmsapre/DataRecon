# Local / developer profile (default).
# App store is PostgreSQL only (results + catalog).
mms:
  recon:
    database:
      host: localhost
      port: 5436
      name: data_recon
      username: postgres
      password: postgres
    postgres:
      datasources: {}
