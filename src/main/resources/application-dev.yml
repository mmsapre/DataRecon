# Local / developer profile (default).
# App store is PostgreSQL only — see mms.recon.database in application.yml
# (results + catalog). Optional empty map below is YAML seed for named
# source/target Postgres connections; prefer POST /api/datasources.
mms:
  recon:
    postgres:
      datasources: {}
