# Optional LLM summary (OpenAI-compatible chat completions).
# Enabled only when both url and api-key are non-blank.
mms.recon.llm.url=${DATA_RECON_LLM_URL:}
mms.recon.llm.api-key=${DATA_RECON_LLM_API_KEY:}
mms.recon.llm.model=${DATA_RECON_LLM_MODEL:gpt-4o-mini}
mms.recon.llm.timeout-seconds=30
mms.recon.llm.max-records=50
